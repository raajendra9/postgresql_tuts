select name from company where name='rishi';
explain select name from company where name='rishi';
-- create index name_index on company(name);
select name from company where name='rishi';
explain select name from company where name='rishi';