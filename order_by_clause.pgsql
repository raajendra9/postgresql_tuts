select * from company order by sal;
select * from company order by age;
select * from company order by age asc;
select * from company order by age desc;
select * from company order by age, sal desc; -- age in asc and sal in desc

-- insert into company values(9, 'rishi', 30, 'banglore',  80000);